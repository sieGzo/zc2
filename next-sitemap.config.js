/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://twojadomena.pl', // <-- podmień na swoją domenę
  generateRobotsTxt: true,           // wygeneruje robots.txt
  sitemapSize: 5000,
}
